export interface EventChoice {
  cards_upgraded: Array<string>;
  damage_healed: number;
  damage_taken: number;
  event_name: string;
  floor: number;
  gold_gain: number;
  gold_loss: number;
  max_hp_gain: number;
  max_hp_loss: number;
  player_choice: string;
}
